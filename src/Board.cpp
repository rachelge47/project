#include "Board.h"

Board::Board()
	:m_level(0)
{}


void Board::loadFromFile()
{
	//get board to auxelary vetor
	
	//convert the auxelary array to m_board (rectangleshape)
}


int Board::getLevel() const
{
	return m_level;
}

void Board::draw(const sf::RenderWindow & window)
{
	//draws the board
}
